const axios = require('axios');

async function testApi() {
  try {
    // Assuming the API is running locally
    // Wait, the API requires a token. It's an admin endpoint.
    // Let's just run it against the backend controller directly, or check the database.
  } catch (err) {
    console.error(err);
  }
}
testApi();
